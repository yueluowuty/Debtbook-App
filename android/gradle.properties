org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# 项目目录含中文，AGP 默认直接拒绝构建（"Your project path contains non-ASCII characters"）。
# 这是被项目目录名逼出来的开关；若后续 aapt2 真的报错，就得把项目挪到纯 ASCII 路径。
android.overridePathCheck=true
# 光关路径检查不够：Kotlin 增量编译在中文路径下无法关闭 cacheable/caches-jvm 里的
# .tab 文件（报错里的路径已经变成乱码），:android_file_picker:compileDebugKotlin 会失败。
# 非增量编译慢一点，但能在这种路径下正常产出。
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
