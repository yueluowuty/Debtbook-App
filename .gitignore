# Miscellaneous
*.class
*.log
*.pyc
*.swp
.DS_Store
.atom/
.build/
.buildlog/
.history
.svn/
.swiftpm/
migrate_working_dir/

# IntelliJ related
*.iml
*.ipr
*.iws
.idea/

# The .vscode folder contains launch configuration and tasks you configure in
# VS Code which you may wish to be included in version control, so this line
# is commented out by default.
#.vscode/

# Flutter/Dart/Pub related
**/doc/api/
**/ios/Flutter/.last_build_id
.dart_tool/
.flutter-plugins-dependencies
.pub-cache/
.pub/
/build/
/coverage/

# Symbolication related
app.*.symbols

# Obfuscation related
app.*.map.json

# Android Studio will place build artifacts here
/android/app/debug
/android/app/profile
/android/app/release

# Widget Preview related
.widget_preview/

# 仅供 `flutter test` 在开发机上加载的真实 SQLite（package:sqlite3 build hook 指向它）。
# 不参与 APK 构建，也不该进版本库。
/sqlite3.dll

# 构建产物（161MB debug 包），别进版本库
/debtbook-debug.apk

# 视觉验收截图（从旧镜像目录抢救回来的，仅本机留档）
/tool/devrun/shots/
